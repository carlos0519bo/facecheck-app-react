import { MainLayout, TableDataList, AlterTable } from '../components';

export const HomePage = () => {
  return (
    <MainLayout>
      <TableDataList />
      {/* <AlterTable /> */}
    </MainLayout>
  );
};
