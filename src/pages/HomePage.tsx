import { MainLayout, TableDataList, Spinner } from '../components';

export const HomePage = () => {
  return (
    <MainLayout>
      <TableDataList />
      {/* <Spinner /> */}
    </MainLayout>
  );
};
